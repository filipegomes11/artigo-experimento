apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pause-scheduler
  namespace: chaos-mesh
spec:
  action: pod-failure
  mode: one
  duration: "90s"
  selector:
    pods:
      kube-system:
      - ${SCHEDULER_POD}
